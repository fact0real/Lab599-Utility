# صدای مستقیم لپ‌تاپ به دیکودر

نسخهٔ 2.173، بیلد 179 از Lab599 Utility گزینهٔ **System Audio (Direct)** را به فهرست ورودی‌های CW اضافه می‌کند.

1. نسخهٔ جدید برنامه را باز کنید.
2. در CW Station، از فهرست ورودی صدا **System Audio (Direct)** را انتخاب کنید.
3. AFC را روشن بگذارید و **START DECODER** را بزنید.
4. اگر macOS درخواست دسترسی داد، اجازهٔ دریافت صدای سیستم را بدهید.
5. فایل مورس را در پخش‌کننده یا مرورگر لپ‌تاپ پخش کنید.

صدا مستقیماً و به‌صورت دیجیتال وارد دیکودر می‌شود؛ پخش معمول بلندگو/هدفون نیز ادامه دارد. میکروفون و نصب درایور صوتی مجازی لازم نیست. صدای همهٔ برنامه‌های در حال پخش، از جمله خود Lab599 Utility، در این ورودی قرار می‌گیرد؛ هنگام آزمون، صداهای غیرمرتبط را متوقف کنید.

این ویژگی از Core Audio Process Tap استفاده می‌کند و به macOS 14.2 یا جدیدتر نیاز دارد. سیستم فعلی macOS 26.7 است. راهنمای API و مجوز: [مستندات Apple](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps).

اگر اجازه رد شده است، بخش **System Settings → Privacy & Security → Screen & System Audio Recording** را بررسی کنید. نام دقیق بخش ممکن است با نسخهٔ macOS فرق کند. اگر فرمت دستگاه خروجی در حال دریافت عوض شود، دیکودر متوقف می‌شود و پیام اتصال مجدد نشان می‌دهد؛ START DECODER را دوباره بزنید.

ورودی USB رادیو و میکروفون همچنان در فهرست هستند. انتخاب System Audio با Refresh یا شناسایی USB جدید خودکار تغییر نمی‌کند. ورودی جدید فقط با شروع دریافت فعال می‌شود و با STOP آزاد می‌شود؛ خروجی پیش‌فرض سیستم تغییر نمی‌کند و در استفادهٔ معمول فایل صوتی ذخیره نمی‌شود. هیچ صوتی بارگذاری نمی‌شود. فقط آزمون صریح خط فرمانِ عیب‌یابی، فایل PCM را کنار گزارش JSON به‌صورت محلی ذخیره می‌کند.

اعتبارسنجی: ۳۰۸ بررسی هسته و فایل‌ها، ۲۰۰ کلمهٔ نویزی و آزمون CW Station موفق شد. هر شش فایل از مسیر واقعی پخش سیستم، بدون بازنشانی دیکودر بین فایل‌ها، دقیق خوانده شدند: OXIDE، FIELD، COLOR، RELAY، JAMMER و COLD. گزارش در `validation/cw-system-audio-2.173-originals.json` است. نسخهٔ universal برای Intel و Apple Silicon ساخته و امضای محلی آن تأیید شد.

نسخهٔ 2.173 قفل‌ماندن AFC روی تُن قدیمی، تولید حروف اضافی از گذرای صوتی پیش از COLD و برگشت وضعیت قدیمی رابط پس از Stop/Clear را اصلاح می‌کند. آزمون‌های جدید شروع پخش را نسبت به پنجرهٔ تحلیل جابه‌جا می‌کنند؛ تغییر اندازهٔ بسته به‌تنهایی این خطا را آشکار نمی‌کرد.

پس از جایگزینی برنامه، نسخهٔ بازِ قبلی را کاملاً Quit و برنامه را از `/Applications/Lab599 Utility.app` دوباره اجرا کنید. پایین نوار کناری باید **v2.173 (Build 179)** نوشته شده باشد.
