# راهنمای Lab599 Utility 2.0

نسخهٔ جدید پنج بخش دارد: به‌روزرسانی فیرمور، همسان‌سازی ساعت، آزمون CAT، پشتیبان‌گیری تنظیمات و مدیریت حافظه. برنامه برای macOS 12 و بالاتر، روی مک‌های Intel و Apple Silicon ساخته شده است. رابط برنامه انگلیسی است.

## شروع کار

فایل `Lab599 Utility.app` را باز کنید، کابل CAT-USB را وصل کنید و پورت مربوط به رادیو را انتخاب کنید. اگر پورت دیده نمی‌شود، Refresh را بزنید. برنامه‌های دیگری را که از همان پورت استفاده می‌کنند ببندید.

چهار بخش Time Sync، CAT Studio، Settings و Memory با روشن‌کردن معمولی رادیو و CAT با سرعت 9600 کار می‌کنند. فقط Firmware Update به حالت Loader نیاز دارد. بازکردن و ویرایش فایل حافظه به اتصال رادیو نیاز ندارد.

## Firmware Update

رادیو را خاموش کنید؛ هنگام روشن‌کردن، سومین کلید بالای دستگاه را نگه دارید تا پیام «The loader is waiting...» دیده شود. فایل `.fw` مناسب مدل دستگاه را با Choose .fw انتخاب کنید یا از بخش Download from Lab599 استفاده کنید. پس از بررسی مشخصات، Update Firmware را بزنید و انتقال را تأیید کنید. تا پایان عملیات برق و کابل را قطع نکنید. پس از موفقیت، دستگاه را دوباره روشن کرده و نسخه را بررسی کنید.

## Time Sync

رادیو را معمولی روشن کنید. بین Mac local time و UTC انتخاب کنید، پیش‌نمایش ساعت را ببینید و Synchronize Clock را بزنید. ساعت سیستم مک باید درست باشد. برنامه ساعت را ارسال و سپس از رادیو می‌خواند؛ فقط پس از تطبیق نتیجه، موفقیت اعلام می‌شود. این فرمان فقط ساعت روز را می‌فرستد، نه تاریخ یا منطقهٔ زمانی. اگر تأیید ناموفق باشد، ممکن است ساعت دستگاه قبلاً تغییر کرده باشد.

## CAT Studio

Ping Once یک بار ارتباط را بررسی می‌کند؛ Continuous Ping آزمون را تا فشردن Stop ادامه می‌دهد. پاسخ شناسه، زمان پاسخ و تعداد موفقیت/خطا نمایش داده می‌شود. مانند TestCAT اصلی، این آزمون فقط فرمان شناسایی را می‌فرستد؛ ارسال رادیویی یا تغییر فرکانس انجام نمی‌دهد. پس از تأیید شناسهٔ رادیو، کنترل‌های فرکانس، مد، توان و فیلتر فعال می‌شوند. فرکانس را می‌توان با جداکنندهٔ هزارگان وارد کرد یا با کلیدهای گام در همان نمایشگر اصلی تغییر داد. گیج S-Meter درجه‌های S1 تا S9 و +20 تا +60 dB دارد؛ هنگام ارسال، مقدار CAT به‌عنوان متر TX نمایش داده می‌شود. پورت را از منوی بالای صفحه انتخاب کنید و پس از وصل کردن مبدل جدید، Refresh را بزنید. کنسول تشخیصی جدا از لاگ CAT است و با دکمهٔ Console باز می‌شود. ترمینال فرمان‌های خام CAT قابلیت تغییر وضعیت رادیو را دارد.

هنگام اجرای Continuous Ping، دکمهٔ Stop کنار کنترل‌های Ping نمایش داده می‌شود. کلید Escape هم آزمون را متوقف می‌کند. بستن پنجره در حین آزمون، ابتدا Ping را متوقف می‌کند و پس از بسته‌شدن پورت سریال از برنامه خارج می‌شود.

دکمهٔ باند متناظر با فرکانس خوانده‌شده از رادیو هایلایت می‌شود و با تغییر فرکانس به‌روزرسانی می‌گردد. بعد از توقف Ping، پیام پایین صفحه وضعیت موجودبودن پورت را جدا از بسته‌شدن نشست سریال بیان می‌کند.

در بخش CAT WORKSPACE، تب CAT Macros برای ساخت دکمه‌های دلخواه است. نام و دستورهای مجاز را، هر دستور در یک خط، وارد کنید و Save Macro را بزنید؛ دکمهٔ آن به ردیف اجرای سریع اضافه می‌شود. نمونهٔ `MD6;`، `PC050;` و `FL1;` مد DIG، توان ۵ وات و فیلتر دریافت ۲ را انتخاب می‌کند. در این بخش `FL1;` میان‌بری است که فیلتر ارسال فعلی را حفظ می‌کند. ماکرو بعد از هر تغییر مقدار را از رادیو می‌خواند و در صورت خطا یا فشردن Stop ادامه نمی‌دهد. فرمان‌های PTT/ارسال در ماکرو مجاز نیستند؛ ترمینال خام جداگانه برای کاربران آشنا به پروتکل باقی می‌ماند.

تب Radio Snapshots همهٔ ۱۰۲۴ بایت تنظیمات و مقادیر زندهٔ قابل‌خواندن را با نام دلخواه ذخیره می‌کند. Restore & Verify ابتدا شناسهٔ مدل را بررسی و یک «Before restore» از تنظیمات فعلی ذخیره می‌کند، سپس تنظیمات را می‌نویسد و همهٔ بایت‌ها را دوباره می‌خواند. بانک ۱۰۰ کانال حافظهٔ `.mem` جزو این Snapshot نیست و در بخش Memory نگهداری می‌شود. هنگام بازیابی کابل و برق رادیو را ثابت نگه دارید؛ توقف میان نوشتن ممکن است بخشی از تنظیمات را تغییر داده باشد.

تب Protocol Monitor ترافیک CAT تولیدشده توسط خود برنامه را با فیلتر TX، RX و خطا، همراه نمودار زمان پاسخ نشان می‌دهد. این تب ترافیک برنامه‌های دیگر را نمی‌شنود، چون پورت CAT در زمان استفاده در اختیار انحصاری برنامه است.

## Settings

برای پشتیبان‌گیری، Read Radio و سپس Save .set را بزنید. فایل اصلی دقیقاً ۱۰۲۴ بایت است. برای بازیابی، Open .set را بزنید و فایل را انتخاب کنید؛ سپس Restore to Radio را بررسی و تأیید کنید. برنامه بعد از نوشتن، همهٔ ۱۰۲۴ بایت را دوباره می‌خواند و مقایسه می‌کند.

این بخش مانند ابزار اصلی، کل مجموعهٔ تنظیمات را نگه می‌دارد؛ نام و معنای هر تنظیم از فایل اصلی مشخص نیست و ویرایش جداگانهٔ آن‌ها ارائه نشده است. فایل قالب اصلی، شناسهٔ مدل یا نسخهٔ فیرمور ندارد؛ قبل از بازیابی، پشتیبان دستگاه خودتان را ذخیره کنید و از منشأ فایل مطمئن باشید. اندازهٔ درست فایل به‌تنهایی سازگاری آن با دستگاه را ثابت نمی‌کند.

## Memory

Read Radio حافظه را می‌خواند؛ Open .mem بانک موجود را باز می‌کند و New Bank یک بانک خالی می‌سازد. جدول شامل ۱۰۰ کانال با شماره‌های 00 تا 99 است. یک ردیف را انتخاب کنید، فرکانس را به هرتز وارد کنید و Mode و PreAtt را تنظیم کنید؛ Apply to Row تغییر را در بانک بازشده ثبت می‌کند و Clear Row آن ردیف را خالی می‌کند.

محدودهٔ ورودی مانند ابزار اصلی از 100000 تا 56000000 هرتز است. حالت‌ها LSB، USB، CW، FM، AM، CWR و DIG هستند. DIG در قالب اصلی کد USB را دارد و پس از بازخوانی USB نمایش داده می‌شود. PreAtt شامل Off، PRE و ATT است.

Save .mem فایل سازگار با ابزار اصلی و با اندازهٔ دقیق ۶۰۰ بایت می‌سازد. Write 100 Channels همهٔ ۱۰۰ کانال رادیو، از جمله ردیف‌های خالی، را جایگزین می‌کند. تغییر جدول به‌تنهایی رادیو را تغییر نمی‌دهد. بعد از نوشتن، فرکانس، حالت و PreAtt تمام کانال‌ها بازخوانی و مقایسه می‌شود. مانند ابزار اصلی، سایر فیلدهای فرمان حافظه با مقادیر ثابت فرستاده می‌شوند؛ این قالب برای حفظ ویژگی‌های اضافی یا نام کانال طراحی نشده است.

## توقف و خطا

در بخش‌های CAT، Settings و Memory دکمهٔ Stop عملیات را متوقف و پورت را آزاد می‌کند. اگر نوشتن شروع شده باشد، توقف یا قطع کابل می‌تواند بخشی از تنظیمات یا حافظه را تغییر داده باشد؛ توقف به معنی بازگرداندن وضعیت قبلی نیست. خواندن ناقص به‌عنوان پشتیبان کامل ذخیره نمی‌شود. برای بررسی خطا، Save Diagnostic Log را بزنید.

## وضعیت این نسخه

ساخت برای هر دو معماری و آزمون‌های شبیه‌سازی انجام شده است. آزمون با رادیوی واقعی هنوز انجام نشده؛ رفتار درایور USB، سیگنال‌های کنترلی حافظه و سازگاری با نسخه‌های مختلف فیرمور نیاز به بررسی عملی دارند. فایل اجرایی امضای محلی دارد و Apple-notarized نیست.

جزئیات شواهد در [گزارش Settings و Memory](CONFIGURATION-REVIEW.md)، [گزارش CAT](CAT-REVIEW.md) و [گزارش آزمون‌ها](validation/RELEASE-2.0.md) آمده است.
